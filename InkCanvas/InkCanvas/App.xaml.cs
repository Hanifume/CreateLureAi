using System;
using System.Collections.Generic;
using System.Configuration;
using System.Data;
using System.Linq;
using System.Windows;

namespace InkCanvas
{
    /// <summary>
    /// App.xaml の相互作用ロジック
    /// </summary>
    public partial class App : Application
    {
        private void Application_Startup(object sender, StartupEventArgs e)
        {
            //Window5 window = new Window5();

            //MessageBox.Show(string.Join(" ", e.Args));

            //if(e.Args.Count() == 3)
            //{
            //    window.CreateModelName = e.Args[0];
            //    window.CreateColorCode = e.Args[1];
            //    window.CreateColorName = e.Args[2];
            //}

            //window.ShowDialog();

        }
    }
}
